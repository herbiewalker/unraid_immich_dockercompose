# Never commit a real env file — only the *.example templates belong in the repo
.env
*.env
!*.example

# Diagnostics / dumps that can leak host details
*.zip
*.sql

# Logs and OS cruft
*.log
.DS_Store
Thumbs.db
